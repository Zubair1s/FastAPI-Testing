this repo helped to practice the fastapi and its working using the uvicorn server and swagger UI 
to run this script install the fastapi framework which is used to create api using python script and uvcorn to run the local server where you can test this 
test the api end points using the swagger UI for testing purpose
